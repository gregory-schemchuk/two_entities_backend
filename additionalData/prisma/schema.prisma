generator client {
      provider = "prisma-client-js"
    }
    datasource db {
      provider = "postgresql"
      url      = env("DATABASE_URL")
    }model AdditionalData{    
id String @id
name String?
score Float?
birthDate DateTime?
}